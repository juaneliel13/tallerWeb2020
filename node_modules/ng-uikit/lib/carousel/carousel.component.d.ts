import { EventEmitter, OnInit } from '@angular/core';
export declare class CarouselComponent implements OnInit {
    items: {
        src: string;
        alt: string;
    }[];
    afterNext: EventEmitter<{}>;
    afterPrev: EventEmitter<{}>;
    activeIndex: number;
    count: number;
    width: string;
    constructor();
    ngOnInit(): void;
    next(): void;
    prev(): void;
    goTo(i: number): void;
}
