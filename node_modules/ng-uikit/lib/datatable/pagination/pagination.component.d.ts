import { EventEmitter, OnInit } from '@angular/core';
import { PagingService } from '../service/paging.service';
export declare class PaginationComponent implements OnInit {
    private _pgs;
    totalCount: number;
    size: number;
    pageChange: EventEmitter<{}>;
    pageCount: number;
    pages: any[];
    pageNum: number;
    totalPages: any[];
    displayNumbers: number;
    lastPage: number;
    constructor(_pgs: PagingService);
    ngOnInit(): void;
    onPageChange(e: any): void;
    shiftBoundary(isReverse?: boolean): void;
    next(): void;
    prev(): void;
}
