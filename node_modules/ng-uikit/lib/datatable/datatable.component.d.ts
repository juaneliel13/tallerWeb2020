import { EventEmitter, OnInit } from '@angular/core';
import { Column, Page, Paging } from './datatable.interface';
import { DatatableService } from './service/datatable.service';
export declare class DatatableComponent implements OnInit {
    private _dts;
    rows: any;
    columns: Column;
    pageChange: EventEmitter<{}>;
    sorted: EventEmitter<{}>;
    size: number;
    page: Page;
    search: any;
    resultCount: number;
    constructor(_dts: DatatableService);
    ngOnInit(): void;
    getColumnClass(col: Column): string;
    onPageChange(e: Paging): void;
    sort(item: any): void;
    dynamicSort(property: any): (a: any, b: any) => any;
    filter(event: any, field: any, val: any): void;
}
